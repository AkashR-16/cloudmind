version: "3.9"

services:
  arangodb:
    image: arangodb:3.11
    environment:
      ARANGO_ROOT_PASSWORD: cloudmind
    ports:
      - "8529:8529"
    volumes:
      - arango_data:/var/lib/arangodb3

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  arango_data:
